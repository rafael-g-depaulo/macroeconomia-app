import styled from "styled-components"

import { textBlue } from "Themes/default"

export const Title = styled.h2`
  font-style: normal;
  font-weight: normal;
  font-size: 24px;
  line-height: 28px;
  text-align: center;

  margin-top: 5px;

  color: ${textBlue};
`

export default Title