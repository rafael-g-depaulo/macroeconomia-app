import styled from "styled-components"

import { fontSize } from "Themes/default"

export const Text = styled.p`  
  font-family: 'Roboto';
  font-style: normal;
  font-weight: normal;
  font-size: ${fontSize};
  line-height: 28px;
  text-align: left;
`

export default Text